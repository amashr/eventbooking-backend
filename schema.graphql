# import * from './generated/graphql-schema/prisma.graphql'

type Query {
  events: [Event]!
}

type Mutation {
  createEvent(
    title: String
    description: String
    image: String
    price: String
  ): Event!
}
