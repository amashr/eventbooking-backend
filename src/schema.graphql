# import * from './generated/prisma.graphql'

type Query {
  events: [Event]!
}

type Mutation {
  createEvent(
    title: String!
    description: String!
    image: String!
    price: Int!
  ): Event!
}
